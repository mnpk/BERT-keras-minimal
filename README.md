Refactored keras bert model which uses the same tokenization as the tensorflow implementation. In addition, we provide a batch generator which maps the raw text to the required input for the BERT model.